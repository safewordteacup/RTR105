#ifndef USERDEFLIB_H
#define USERDEFLIB_H

void nanr(void);//no arguments, no return
void anr(int, int);//with arguments, but no return
int nar(void);//no arguments, but with return
long long int ar(long long int);//both with arguments and return

#endif //USERDEFLIB_H